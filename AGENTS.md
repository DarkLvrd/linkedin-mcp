# AGENTS.md

## Agent skills

### Issue tracker

Issues, the wayfinder map, and tickets live as markdown files under `.scratch/` (local-markdown tracker). See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context: `CONTEXT.md` at the repo root + `docs/adr/` when an ADR is warranted. See `docs/agents/domain.md`.
